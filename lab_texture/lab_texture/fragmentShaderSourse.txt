#version 330 core

in vec3 ourColor;
in vec2 ourTex;

out vec4 color;

uniform sampler2D ourTexture1;
uniform sampler2D ourTexture2;

void main()
{
	color = mix(texture(ourTexture1, ourTex), texture(ourTexture2, ourTex), 0.2);
};